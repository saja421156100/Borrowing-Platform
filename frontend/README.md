# Borrowly Frontend

The frontend is plain HTML + Tailwind CDN + JavaScript and is fully connected to the Laravel API in `../backend`.

For the complete setup and test flow, see [`../README_FINAL.md`](../README_FINAL.md).

Default API base: `http://127.0.0.1:8000/api`.
